{[ let token = context /]}
{[ let configuration = exportConfiguration() /]}
{[ let showReferences = configuration.blockConfigTokensShowReferences /]}

{* Display token with or without references using helper functions *}
{[ if showReferences ]}
    {{ displayTokenWithReferences(token, true) }}
{[ else ]}
    <div class="token-value">{{ displayTokenValue(token) }}</div>
{[/]}